# ============================================================================
# Open WebUI — Slim requirements for DOU Assistant
# Removed: torch, transformers, sentence-transformers, whisper, ONNX, cloud SDKs,
#          unused vector DBs, enterprise auth, telemetry, heavy doc parsers
# ============================================================================

# Core web framework
fastapi==0.135.1
uvicorn[standard]==0.41.0
pydantic==2.12.5
python-multipart==0.0.22
itsdangerous==2.2.0

# WebSocket & Auth
python-socketio==5.16.1
python-jose==3.5.0
cryptography
bcrypt==5.0.0
argon2-cffi==25.1.0
PyJWT[crypto]==2.11.0
authlib==1.6.9

# HTTP clients
requests==2.32.5
aiohttp==3.13.2 # do not update to 3.13.3 - broken
async-timeout
aiocache
aiofiles
starlette-compress==1.7.0
Brotli==1.1.0
httpx[socks,http2,zstd,brotli]==0.28.1
starsessions[redis]==2.2.1
python-mimeparse==2.0.0

# Database (PostgreSQL)
sqlalchemy==2.0.48
alembic==1.18.4
peewee==3.19.0
peewee-migrate==1.14.3
psycopg2-binary==2.9.11

# Collaboration
pycrdt==0.12.47
redis

# Scheduling & security
APScheduler==3.11.2
RestrictedPython==8.1
pytz==2026.1.post1

# Logging
loguru==0.7.3
asgiref==3.11.1

# AI libraries (remote API only — no local models)
tiktoken
openai
mcp==1.26.0

# RAG framework (needed by OpenWebUI internals)
langchain==1.2.10
langchain-community==0.4.1
langchain-classic==1.0.1
langchain-text-splitters==1.1.1

# Vector DB (default — chromadb is lightweight and needed by OpenWebUI core)
chromadb==1.5.2

# Document parsing (minimal set)
fake-useragent==2.2.0
ftfy==6.3.1
chardet==5.2.0
pypdf==6.7.5
fpdf2==2.8.7
Markdown==3.10.2
beautifulsoup4
pymdown-extensions==10.21
validators==0.35.0
psutil
pillow==12.1.1

# Code formatting (used by UI code editor)
black==26.1.0

# Search
rank-bm25==0.2.2
ddgs==9.11.3

# Web search
pydub

# Database extras
PyMySQL==1.1.2
